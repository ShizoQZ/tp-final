function click(posX, posY, tamX, tamY) { //funcion con parametros, agregué tamX y tamY para flexibilidad
return mouseX > posX && mouseX < posX + tamX &&
mouseY > posY && mouseY < posY + tamY;
}

//no tocar nada
