function PasarPantalla(pantallaActual, pantallaProx) {
if (Pantalla === pantallaActual) {
Pantalla = pantallaProx;
}
}

// Corregí el bug: agregué el && faltante y quité el //2 que parecía un error (asumí tam completo, ajusta si querías /2).
